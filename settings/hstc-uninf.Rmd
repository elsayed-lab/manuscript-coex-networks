```{r}
# H. sapiens infected with T. cruzi (Uninfected samples only)
input_dir <- file.path(Sys.getenv('SCRATCH'),
                       'param_opt/normal/hsapiens_infected_with_tcruzi-uninf-v6.0')

# Output sub-directory
outdir <- 'hstc-uninf-v6.0'

# Networks to include
nets <- tbl_df(read.csv(file.path(Sys.getenv('r15'),
                                  '115-coex-network-param-opt-v6', 'output',
                                  'normal',
                                  'hsapiens_infected_with_tcruzi-uninf-v6.0.csv')))
net_filenames <- paste0(nets$id, '_adjmat.RData')

# setup expected CONFIG object 
CONFIG <- list(
    include_tables=TRUE,
    include_plots=FALSE,
    verbose=TRUE,
    debug=FALSE,
    use_cache=FALSE,
    target="host",
    host="H. sapiens",
    pathogen="T. cruzi",
    organism_db="Homo.sapiens",
    organism_genome="hg19",
    orgdb_key="ENSEMBL",
    biomart_dataset= "hsapiens_gene_ensembl"
)
```
